let myBook = {
    title: 'richdadpoordad',
    author: 'mohame douzi',
    pageCount: 326
}

console.log(`${myBook.title} by s${myBook.author}`)

myBook.title = 'animal farm'

console.log(`${myBook.title} by s${myBook.author}`)

// challenge area

// name, age, location

// mohamed is 22 lives in Morocco

let user = {
    name: 'mohamed',
    age: 22,
    location: 'Morocco'
}


console.log(`${user.name} is ${user.age} lives in ${user.location}`)
user.age = user.age * 1
console.log(`${user.name} is ${user.age} lives in ${user.location}`)

